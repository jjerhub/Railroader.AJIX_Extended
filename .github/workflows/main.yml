name: Do zip on release
on:
  release:
    types:
      - published
  workflow_dispatch:
      
jobs:
  do_zip:
    secrets: inherit
    uses: jjerhub/Utilities.GithubWorkflows/.github/workflows/ZipContents.yml@master
    with:
      zip-file-name: 'AJIX_Extended'
